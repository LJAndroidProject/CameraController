<?xml version="1.0" encoding="utf-8"?>
<resources>

    <attr name="HomeBackAndRightButtonTheme" format="reference" />
    <attr name="LargeFilletButton" format="reference" />

    <declare-styleable name="MsgView">
        <!-- 圆角矩形背景色 -->
        <attr name="mv_backgroundColor" format="color" />
        <!-- 圆角弧度,单位dp-->
        <attr name="mv_cornerRadius" format="dimension" />
        <!-- 圆角弧度,单位dp-->
        <attr name="mv_strokeWidth" format="dimension" />
        <!-- 圆角边框颜色-->
        <attr name="mv_strokeColor" format="color" />
        <!-- 圆角弧度是高度一半-->
        <attr name="mv_isRadiusHalfHeight" format="boolean" />
        <!-- 圆角矩形宽高相等,取较宽高中大值-->
        <attr name="mv_isWidthHeightEqual" format="boolean" />
    </declare-styleable>

    <declare-styleable name="titleView">
        <!--左上角图片-->
        <attr name="leftImage" format="reference"></attr>
        <!--左上角图片是否显示-->
        <attr name="leftImageVisible" format="boolean"></attr>
        <!--左上角文字-->
        <attr name="leftText" format="string"></attr>
        <!--标题内容-->
        <attr name="titleContent" format="string"/>
        <!--右上角图片-->
        <attr name="rightImage" format="reference"></attr>
        <!--右上角图片2-->
        <attr name="rightImage2" format="reference"></attr>
        <!--右上角图片是否显示-->
        <attr name="rightImageVisible" format="boolean"></attr>
        <!--右上角文字-->
        <attr name="rightText" format="string"></attr>
        <!--是否拦截返回按钮-->
        <attr name="interceptOrReturn" format="boolean"></attr>
    </declare-styleable>
</resources>